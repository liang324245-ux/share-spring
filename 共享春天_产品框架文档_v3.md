# 《共享春天》微信小程序 - 完整产品框架文档 v3.0

> **本版定位**：v2.0 是"界面完成、待接入后端"的设计稿；**v3.0 是后端全部实现完成后的留档**，记录真实落地的架构、云函数、数据结构与所有新增设定。文末附 v3.0 修订摘要。

---

## 📱 产品定位

**产品名称**：共享春天（英文标识：LITTLE MOMENTS）
**核心理念**：低社交负担分享
**目标用户**：有社交负担的 i 人，想告诉朋友"我仍在惦记你"，但不强制要求回应
**产品特色**：朋友可随时登录查看分享，无需实时回复压力
**技术栈**：微信小程序 + 微信云开发（WeChat Cloud Development）
**云环境**：`cloudbase-d3g00thv5d1e1d96e`

---

## 🎯 核心功能（已全部实现）

1. **一对一照片分享** —— 与朋友独立的分享空间
2. **时间隔离日历** —— 按拍摄日期查看照片，沉浸式体验
3. **轻量评论** —— 自愿的、非强制的互动（已持久化）
4. **点赞** —— 接收者对照片点赞，上传者可见（已持久化）
5. **已读标志** —— 了解朋友是否看过
6. **暗号添加朋友** —— 安全、私密的朋友认证
7. **灵活删除选项** —— 4 种情况 + 3 天定时清理
8. **好友备注名**（⭐ 新增）—— 给朋友起只有自己能看到的称呼

---

## 🏗️ 技术架构

### 云函数清单（共 10 个）

| 云函数 | 类型 | 功能 |
|--------|------|------|
| `login` | 普通 | 登录 / 建用户档案，返回 isNew 与 userInfo |
| `createInvite` | 普通 | 创建暗号邀请 |
| `matchInvite` | 普通 | 匹配暗号、连接好友 |
| `updateProfile` | 普通 | 更新昵称 / 头像 |
| `getPhotoUrls` | 普通 | 批量把 `cloud://` 换成 https 临时链接（全项目复用） |
| `markRead` | 普通 | 标记照片已读（仅接收者可标） |
| `addComment` | 普通 | 添加评论（仅照片相关方可评） |
| `toggleLike` | 普通 | 接收者点赞 / 取消（仅接收者可点） |
| `deleteFriend` | 普通 | 删除朋友（软删除 + 可选清除照片） |
| `cleanExpired` | **定时** | 每天 0 点扫描超 3 天的 B2 关系，置为 expired |
| `setRemark` | 普通 | 设置好友备注名（存关系记录） |

> 实际为 11 项（含定时函数）。下文凡涉及"跨用户读写数据"，一律走云函数（管理员权限），因为前端只能改自己创建的记录。

### 关键架构决策（踩坑沉淀）

1. **云存储权限**：上传的文件默认"仅创建者可读"。跨用户看图（朋友头像、对方照片）若前端直接 `wx.cloud.getTempFileURL` 会报 `STORAGE_EXCEED_AUTHORITY`。**必须走云函数 `getPhotoUrls`**（云函数有管理员权限）批量换临时链接。
2. **集合读权限**：`comments`、`likes` 等需跨用户读取的集合，权限要设为"**所有用户可读**"，否则写得进、读不出。
3. **数据库语法限制**：不支持 `_.in` / `_.nin`、不支持 ES6 展开运算。多 id 查询用循环逐个查，或前端过滤。
4. **前端异步串行**：photoday 同时加载"换链接 / 评论 / 点赞"时，多个异步 `setData` 会互相覆盖。**改为串行回调**（先渲染照片 → 再评论 → 再点赞），每步基于最新 `this.data.zones` 增量更新。
5. **静态 UI 图片**：本地存放 + tinypng 压缩（主包须 < 1.5M），不放云存储。
6. **完整文件替换**：开发中一律提供完整文件而非局部 diff，避免手动合并的括号 / 语法错误。

---

## 📊 数据结构（实际字段）

### 【users 用户表】
```
- _id
- openid          微信 openid
- nickname        昵称（新用户初始为空，引导去设置页填）
- avatar          头像（cloud:// 地址）
- created_at
```

### 【friendships 朋友关系表】
```
- _id
- user_a          发起方 openid
- user_b          接收方 openid
- status          connected / deleted / expired
- secret_upper    暗号上句
- secret_lower    暗号下句
- initiator_part  发起者输入的是上句还是下句
- created_at
- connected_at

⭐ 删除逻辑字段：
- deleted_by      谁发起的删除（openid）
- keep_memory     删除方是否为对方保留回忆（true=保留→B1 / false=清除→B2）
- left_at         删除/变灰时间（定时清理判断依据）

⭐ 备注名字段（新增）：
- remark_a_for_b  user_a 给 user_b 起的备注（只 a 可见）
- remark_b_for_a  user_b 给 user_a 起的备注（只 b 可见）
```

### 【photos 照片表】
```
- _id
- uploader          上传者 openid
- receiver          接收者 openid ⭐ 一对一关键字段
- shoot_date        拍摄日期 "YYYY-MM-DD" ⭐ 日历按此归类
- upload_time       上传时间 ⭐ 同天双方都传时判断"谁最新"（定圆点颜色）
- image_urls        图片 cloud:// 列表
- caption           文案（可选）
- is_read_by_receiver  接收方是否已读 ⭐ 计算未读/圆点/置顶卡片
```

### 【comments 评论表】（权限：所有用户可读）
```
- _id
- photo_id      照片 _id
- commenter     评论者 openid
- content       内容（≤200字）
- created_at
```

### 【likes 点赞表】（权限：所有用户可读）
```
- _id
- photo_id      照片 _id
- liker         点赞者 openid（只可能是接收者）
- created_at
```

> 点赞规则：一张照片 + 接收者 = 最多一条记录。点赞=新增，取消=删除该记录。

---

## 🎨 页面结构（9 个页面）

页面：login / guide / friends / calendar / photoday / upload / addfriend / settings / delfriend

### 【1. 登录页 login】（⭐ 已简化）
```
├── 背景图 + 标题图 + 副标题
└── 【进入】按钮（直接微信登录，不再输入名称）

登录后路由：
├── 有昵称（老用户）→ 直接进首页 friends
└── 无昵称（新用户）→ 引导页 guide
```

### 【2. 首次引导页 guide】
```
4 张功能介绍卡片 → 【开始使用】
└── ⭐ 结束后跳【设置页 settings?firstTime=1】（强制新用户先填资料）
```

### 【3. 朋友列表页 friends】（首屏）
```
顶部：标题"分享我们的 LITTLE MOMENTS" + ⚙️设置入口

置顶大卡片（⭐ 规则已更新）：
├── 显示"朋友发我的、且我未读的"最新一张照片
├── ⭐ 已读后不再占据卡片位
├── ⭐ 已离开（被对方删除）的人发的照片不占卡片位
└── 无符合条件时 → 空状态"或许此刻你想上传点什么给你朋友呢~"

朋友列表：
├── 头像 + 名字（⭐ 有备注显示备注，无备注显示真实昵称）
├── 正常朋友 → 点击进 calendar
├── 我主动删的朋友 → 不显示
├── 被对方删（B1）→ 灰头像，可点进只读 calendar
├── 被对方删（B2）→ 灰头像，点击提示"对方已离开"
└── 【+ 添加】按钮

⭐ B1 离开提示：进首页时，对"保留式删除我"的关系弹一次告别提示
   （本地缓存 left_notified 去重，每条只弹一次）
```

### 【4. 日历页 calendar】（核心）
```
顶部：返回 ‹ ｜ 朋友名字 + ✎笔图标（⭐ 点击改备注）｜ 🗑️删除

日历网格（默认当前月份 ⭐）：
├── 圆点颜色 = 谁上传：🟨黄=我 / 🟦蓝=朋友 / 双方取最新上传方
├── 数字颜色 = 我有无未读：🔴红=有朋友发的我没读 / 正常=无未读
└── 点日期 → 进 photoday

⭐ 上传按钮：跟随日历高度浮动在卡片下方（固定相对距离）
⭐ 只读模式（readonly，B1 进入）：隐藏上传按钮，仅看历史

底部图例：黄点=我 / 蓝点=朋友 / 红字=未读
```

### 【5. 照片详情页 photoday】
```
按 shoot_date 显示当天双方照片，每张为一个 zone：
├── 发布者名字（⭐ 备注优先）+ 头像
├── 照片组（点击放大 lightbox）
├── 文案
├── 操作行：❤️/🤍 点赞 ｜ 💬 评论数（⭐ 可点切换评论框）｜ ✓✓✓（我发的+已读）
├── 评论区（有评论时显示，显示评论者昵称/我）
└── ⭐ 评论框：默认隐藏，点💬才出现，发送后自动收起

点赞规则：
├── 接收者：爱心可点，显示自己赞没赞，持久化
└── 上传者：爱心只读，显示"接收者赞没赞"，点击提示"这是对方的心意哦"

进页面自动对"朋友发我的未读照片"标已读
```

### 【6. 上传页 upload】（⭐ 已优化）
```
├── 选择照片：⭐ 无占位框，直接"＋"添加（已选 N 张）
├── 拍摄日期：⭐ 默认今天，可前后调
├── 文案（可选，≤200字）
└── 【立即分享】
```

### 【7. 添加朋友页 addfriend】
```
暗号系统：发起者输入上下句之一 + 选择是上句还是下句 → 生成邀请
接收者输入另一句 → 匹配成功连接
（暗号支持中英文逗号切分）
```

### 【8. 删除朋友页 delfriend】（⭐ 两种进入模式）

**模式一：正常删除**（A 删 B，从 calendar 🗑️ 进入）
```
头像（⭐ 真实朋友头像）+ "确定要离开吗？"

○ 通知对方，并为对方保留回忆 → 对方进入 B1
○ 彻底清除双方回忆 → 对方进入 B2
【确认删除】【再想想】
```

**模式二：B1 清理**（⭐ 新增，B 被保留式删除后，从只读 calendar 🗑️ 进入，带 cleanup=1）
```
头像 + "你确定要彻底删除你们的回忆吗？"
说明："○○已经离开。删除后照片与评论将彻底消失，无法恢复。"

【是，彻底删除】（固定走彻底清除）
【否，再留一会儿】
```

### 【9. 设置页 settings】（⭐ 已实现 + 新流程）
```
个人信息：
├── 头像（⭐ 白底框；没传时显示"＋"，不再用 emoji；点击 chooseAvatar 上传）
├── 昵称（点击填写，type=nickname）
└── 已进入小世界 N 天

消息提醒开关（评论提醒 / 新照片提醒）
账号与安全（隐私设置 / 微信绑定 / 账号注销）
退出登录

⭐ firstTime 模式（新用户首次）：必须填昵称才能进入主流程
```

---

## 🗑️ 删除逻辑（4 种情况 + 定时清理）

删除由两维度决定：**谁删的** + **删除方是否为对方保留回忆**。

### 删除对照表

| 谁删的 | 删除方选择 | 被删方看到 |
|--------|-----------|-----------|
| 我删朋友 | 为对方保留 | 对方 = B1（永久灰 + 只读回忆） |
| 我删朋友 | 彻底清除 | 对方 = B2（灰 3 天后自动移除 + 照片清除） |
| 朋友删我（B1） | 我再"彻底删除" | 照片清除，朋友从我列表移除 |
| 朋友删我（B2） | — | 灰 3 天 → cleanExpired 自动移除 |

### 实现要点

1. **软删除**：删除不删 friendships 记录，而是改 `status='deleted'` + 记 `deleted_by`/`keep_memory`/`left_at`。这样被删方还能查到关系、显示灰头像。
2. **我主动删的**：列表里跳过不显示；被对方删的：灰显。
3. **彻底清除**：deleteFriend 在 clear 模式下，连带删除两人之间的 photos 及其 comments、likes。
4. **B1 只读**：calendar 接 `readonly=1`，隐藏上传按钮、onUpload 拦截。
5. **B1 清理**：calendar 在 readonly 下点🗑️，带 `cleanup=1` 给 delfriend，显示"是/否"文案，固定走 clear。deleteFriend 云函数已放宽到可处理 `deleted` 状态的关系。
6. **定时清理（cleanExpired）**：定时触发器 cron `0 0 0 * * *`（每天 0 点），扫描 `status=deleted` + `keep_memory=false` + `left_at` 超 3 天的关系，置为 `expired`。前端只查 connected/deleted，故 expired 自动从列表消失。

---

## 📍 日历颜色逻辑（关键）

### 维度一：圆点颜色 = 谁上传
- 🟨 黄点 = 我上传 ｜ 🟦 蓝点 = 朋友上传 ｜ 同天双方都传 → 取最新上传方颜色（按 upload_time）

### 维度二：数字颜色 = 我有无未读
- 🔴 红字 = 这天有"朋友发的、我还没读的" ｜ 正常 = 无未读
- ⚠️ 未读只看"朋友发的"。我自己发的不论对方读没读，都不让我的数字变红。

### 详情页 ✓✓✓
- 我发的、朋友已读 → 显示 ✓✓✓（绿）
- 朋友发的 → 不显示 ✓✓✓（已读与否由日历数字体现）

---

## 🏷️ 好友备注名（⭐ 新增功能）

**理念**：用户可以给朋友起一个**只有自己能看到**的称呼，不影响朋友真实昵称、不影响他人所见。类似微信好友备注。

**入口**：日历页顶部，点朋友名字或右侧 ✎ 笔图标 → wx.showModal 弹输入框。

**存储**：存在 friendships 关系记录（`remark_a_for_b` / `remark_b_for_a`，按"我是 a 还是 b"存对应字段）。云函数 `setRemark` 负责写入（≤20字，带关系校验）。

**显示优先级**：有备注显示备注，无备注显示真实昵称。生效于——
- ✅ 首页朋友列表的名字
- ✅ 日历页顶部名字
- ✅ photoday 里"谁发的"
- ⚠️ 首页置顶大卡片名字暂未接备注（仍显示真实昵称）—— 已知小不一致，待补

**边界**：备注填了又清空 → 恢复显示朋友真实昵称（calendar 预先查存了真实昵称用于回退）。

---

## 🔄 核心用户流程

### 新用户流程（⭐ 已更新）
```
1. 微信登录（不输名称）
2. 引导页（4 卡）
3. ⭐ 自动跳设置页填昵称+头像（强制）
4. 进首页 → 添加朋友（暗号）
5. 进日历 → 上传/查看
```

### 老用户流程
```
进 app → 自动登录 → 直接进首页
```

### 查看朋友分享
```
首页置顶卡片（有未读时）/ 朋友列表 → 进日历 → 点红字日期 → photoday 自动标已读 → 红字消失
```

---

## ✨ 核心设计原则

1. **无社交负担**：列表显示动态但无强制回应；评论/点赞自愿；只显示拍摄日期不显示上传时间。
2. **沉浸式**：日历按拍摄日期、时间隔离、专注当天。
3. **私密性**：一对一无对比；暗号添加；备注只己可见；删除完全用户掌控。
4. **温暖感**：文案温和（"一段关系的告别""这是对方的心意哦"）；春天主题；尊重各种删除选择。

---

## 🎨 视觉方向

- 配色：春天色系（嫩绿、樱花粉、阳光黄、天蓝），水彩 / 手绘 / 治疗系，绿色调
- 风格：低压力、留白、克制装饰
- 当前进度：核心功能与后端全部完成，前端基础设计完成

---

## 📌 待办 / 已知事项

- ⚠️ 置顶大卡片名字未接备注（小不一致）
- ⬜ 各页空白处插画补充（留到所有布局调整完成后）
- ⬜ 加好友"等待确认中"状态（可选优化）
- ⬜ 表情反应（预留，暂不实现）

---

**文档版本**：v3.0
**最后更新**：2026.6.19
**产品阶段**：✅ 核心功能 + 后端全部实现完成；前端美化与边角打磨中

---

## 📝 v3.0 修订摘要（相对 v2.0）

1. **后端全部落地**：列出 11 个云函数（含定时 cleanExpired）、真实数据结构字段、云开发踩坑沉淀（云存储权限、集合读权限、_.in 限制、异步串行）。
2. **登录流程简化**：登录页去掉"输入名称"；新用户走 引导页→设置页（强制填资料）；老用户直接进首页（按有无昵称判断）。
3. **评论 / 点赞落地**：评论持久化（addComment + comments 集合）；点赞持久化（toggleLike + likes 集合，接收者点、上传者只读可见）。
4. **photoday 交互**：💬 改为可点开关，评论框默认隐藏、点击出现、发送收起。
5. **删除逻辑完整化**：B1 只读日历 + 上传隐藏；B1 离开进首页提示（本地去重）；新增 **B1 清理模式**（delfriend 两套文案）；cleanExpired 定时清理 B2。
6. **置顶卡片规则**：只显示未读照片；已读后 / 对方已离开均不占卡片位。
7. **⭐ 新增好友备注名**：日历页点名字 / ✎ 改备注，存 friendships，只己可见，列表 / 日历 / photoday 备注优先。
8. **UI 细节优化**：日历默认当前月份；上传页取消占位框、日期默认今天、上传按钮随日历浮动；设置页头像白底 + "＋"占位（去 emoji）；delfriend 显示真实朋友头像。
9. **工程**：图片 tinypng 压缩控制主包 < 1.5M；修正前端误 require('wx-server-sdk') 导致白屏的问题。
